properties([[$class: 'GithubProjectProperty', displayName: '', projectUrlStr: 'https://github.com/charan9318/charan_9318.git/'], pipelineTriggers([githubPush()])])
pipeline {
    agent any 

    stages {
        stage('Build_dh') { 
            steps { 
                sh 'ls' 
            }
        }
        stage('Test_dh'){
            steps {
            sh 'ls'
            }
        }
        stage('Deploy_dh') {
            steps {
                sh 'pwd'
            }
        }
    }
}
